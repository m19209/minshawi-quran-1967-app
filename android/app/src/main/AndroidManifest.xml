<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions for streaming Quran recitations -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground service and audio playback permissions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Exact Alarm permissions for precise Azan timing -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Location permissions (optional for auto-detecting country/city) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Storage permission for downloading recitations on Android 9 and lower -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:name=".MinshawiApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MinshawiQuran1967"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Media notification small icon for lock screen and status bar -->
        <meta-data
            android:name="androidx.media3.session.NOTIFICATION_ICON"
            android:resource="@drawable/ic_graphic_eq" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.MinshawiQuran1967">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MediaSessionService for Quran Playback -->
        <service
            android:name=".audio.QuranAudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- Azan Foreground Service for reliable sound in background/lockscreen -->
        <service
            android:name=".prayer.AzanPlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />

        <!-- BroadcastReceiver for Prayer Time Alarms -->
        <receiver
            android:name=".prayer.AzanReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.minshawi.quran1967.ACTION_AZAN_ALARM" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver to reschedule Azans upon device restart -->
        <receiver
            android:name=".prayer.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
